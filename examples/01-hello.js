// console is actually defined in v8,
// but it doesn't send it anywhere. In a pure
// v8 this won't error. It won't do anything either.

// Which means the canonical "hello world" javascript
// app isn't the best first demo of v8.
console.log("hello")